	// liste des dossiers
	var anFolderTab = new folderTab();
	
	jQuery(document).ready(function() {
		// verification identification prealable si portail prive
		/*if (isPrivate())
			return;*/
		
		// Parametrage de la liste des videos les plus recentes
		$('#myCarousel').carousel({interval:false});
	});
	